export function emailValidator () {
  const emailInput = document.getElementById('loginEmailInput')

  emailInput.addEventListener('focusout', (event) => {
    const emailText = event.target.innerText
    /*
    invalid email input
        syntax
        
    */
  })
}
